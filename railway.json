{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd fake_image_detector && gunicorn --bind 0.0.0.0:$PORT app:app"
  }
}
