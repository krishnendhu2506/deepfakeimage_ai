[phases.setup]
nixPkgs = ["python311", "gcc", "stdenv.cc.cc", "zlib"]

[phases.install]
cmds = ["python -m pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "gunicorn wsgi:app"